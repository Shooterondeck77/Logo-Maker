const { questions } = require('./lib');
const { prompt } = require('inquirer');

prompt(questions).then(ans => {
    console.log(ans);
})
class Svg{
constructor(){
    this.textElement = ''
    this.shapeElement = ''

}
render

}